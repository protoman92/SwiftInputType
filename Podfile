# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftInputType' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftInputType
  pod 'SwiftUtilities', :git => 'https://github.com/protoman92/SwiftUtilities.git'

  target 'SwiftInputTypeTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'SwiftUtilitiesTests', :git => 'https://github.com/protoman92/SwiftUtilities.git'
  end

end
